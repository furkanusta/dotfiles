# -*- mode: snippet -*-
# name: ifndef
# key: ifndef
# --
#ifndef ${1:`
        (upcase
            (concat
                (replace-regexp-in-string " \\|\\-" "_" (projectile-project-name))
                "_"
                (replace-regexp-in-string
                    "/\\|\\.\\|\\-"
                    "_"
                    (file-relative-name
                        buffer-file-name
                        (concat
                            (projectile-project-root)
                            "include")))))`}
#define $1

$0

#endif // $1